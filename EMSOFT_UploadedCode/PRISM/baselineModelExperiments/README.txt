The code in this folder creates the LEC models for the AEBS experiments.

To create the LEC models, run any of the python scripts. At the top of the scipts you will need to set the desirbed L (number of detections needs in the window) and H (window size) values

Call visulizeTradeoffs.py to visualize the data for figure 